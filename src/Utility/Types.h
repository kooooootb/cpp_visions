#ifndef VISIONS_TYPES_H
#define VISIONS_TYPES_H

using posm_t = double; // (position metric) type for placing sprites, calculating positions etc
using txtm_t = int; // (texture metric) type for texturing (should be integral as pictures are bitmap)

#endif //VISIONS_TYPES_H
